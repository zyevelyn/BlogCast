<% if post.comments.count == 0 %>
<%= link_to "Leave your Comments", post_path(post, :anchor => "comments") %>
<% else %>
<%= link_to pluralize(post.comments.count, "comments"), post_path(post, :anchor => "comments") %>
Leave your footprint here <%= post.comments.all.last.name %>
<% end %>